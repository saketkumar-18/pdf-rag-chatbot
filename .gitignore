# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local data (rebuildable)
data/chroma/
data/uploads/

# Bundled embedding-model cache for serverless cold starts (87MB ONNX
# weights — too big for git; `vercel --prod` bundles it from local disk,
# fastembed auto-downloads it for everyone else)
assets/

# Secrets
.env

# Build artifacts
report_build/
spaces/_build/

# Local tooling
.vercel/
