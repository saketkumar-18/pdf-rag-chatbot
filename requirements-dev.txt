# Dev/test dependencies (not needed to run the deployed app).
# Runtime deps: requirements.txt (local) / api/requirements.txt (Vercel).
-r requirements.txt
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
fpdf2>=2.7           # builds the demo PDF fixture in tests/
